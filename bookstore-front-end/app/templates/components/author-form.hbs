<h2>Add new book</h2>

<form {{action 'submit' on='submit'}}>
  <div class="form-group">
    <label for="">Name</label>
    {{input class="form-control" value=author.name name="author[name]"}}
  </div>
  <button class="btn btn-primary" type="submit">{{buttonLabel}}</button>
</form>