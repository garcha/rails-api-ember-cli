<ul>
  {{#each model as |author|}}
   <h3>{{author.name}}</h3>
     {{#each author.books as |book|}}
     <p>Books: {{book.title}}</p>
     <p>Publisher: {{book.publisher.name}}</p>
     {{/each}}
     <br>
   {{/each}}
</ul>